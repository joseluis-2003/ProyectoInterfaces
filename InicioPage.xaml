<Page x:Class="Proyecto2TrimestreInterfaces.InicioPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Proyecto2TrimestreInterfaces"
      xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="InicioPage">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Texto Pestaña -->
        <StackPanel Grid.Row="0"
                    Orientation="Horizontal"
                    VerticalAlignment="Top">
            <fa:IconImage Icon="Home"
                          Height="20"
                          Width="20"
                          Foreground="{StaticResource titleColor2}"
                          Margin="35,0,10,0"/>

            <TextBlock Text="Inicio"
                       Foreground="{StaticResource titleColor2}"
                       FontSize="16"
                       FontFamily="Montserrat"
                       FontWeight="Medium"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Texto Bienvenida -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">
            <fa:IconImage Icon="User"
                          Height="35"
                          Width="35"
                          Foreground="{StaticResource titleColor3}"
                          Margin="0,0,10,0"/>
            <TextBlock x:Name="txtBienvenido"
                       Text="Bienvenido, "
                       Foreground="{StaticResource titleColor2}"
                       FontSize="25"
                       FontFamily="Montserrat"
                       FontWeight="Medium"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Info Aplicacion -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Height="350"
                    Width="750">

            <TextBlock TextWrapping="Wrap"
                       Foreground="{StaticResource titleColor2}"
                       FontSize="30"
                       Width="750">
                <Run Text="Bienvenido a Nuestro Proyecto 🌟" FontWeight="Bold" FontSize="22"/>
                <LineBreak/>

                <Run Text="Descripción: " FontWeight="Bold" FontSize="18"/>
                <Run Text="JoséRic Apps fue creada por José Luis y Ricardo para destacar las características de desarrollo con WPF y .NET. Ofrece una interfaz intuitiva y funciones esenciales para una experiencia fácil y práctica." FontSize="18"/>
                <LineBreak/>


                <LineBreak/>
                <Run Text="Saludos,El equipo de José Luis y Ricardo"
                     FontWeight="Bold"/>
                <LineBreak/>
                
                
                

                
                
            </TextBlock>
        </StackPanel>
    </Grid>
</Page>
